services:
  - type: web
    name: ooh-proposal-generator
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app
    envVars:
      - key: ANTHROPIC_API_KEY
        sync: false        # You'll paste the value in the Render dashboard
      - key: FLASK_DEBUG
        value: "0"
